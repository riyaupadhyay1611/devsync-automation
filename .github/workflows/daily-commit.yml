name: Daily Automated Commit

on:
  schedule:
    - cron: '30 2 * * *'   # Runs daily at 02:30 UTC
  workflow_dispatch:         # Allows manual trigger

jobs:
  daily-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit update for 23f2005466@ds.study.iitm.ac.in
        run: |
          git config --global user.email "23f2005466@ds.study.iitm.ac.in"
          git config --global user.name "DevSync Automation"
          echo "Daily update: $(date '+%Y-%m-%d %H:%M:%S')" >> daily_log.txt
          git add daily_log.txt
          git commit -m "Daily automated commit - $(date '+%Y-%m-%d')"
          git push

